# Bunsho — Decisions

Why the code is the way it is.

Append settled decisions here with a date and rationale. Source comments cite this file by section.

**Thesis:** *git inside, Google-Docs outside.* Ship the document-control slice extremely well — immutable versions, audit-shaped data model, tamper-evident log, portable Markdown — behind a fast, friendly editor with zero git vocabulary. Positioning: *a great docs tool that happens to satisfy auditors*, not a GRC suite. Anti-lock-in promise: *your QMS is a folder of Markdown you can walk away with.*

## Roles

| Role | Needs |
|---|---|
| **Admin** | Full control: create/deactivate users, manage roles, delete documents, view/export audit log and version history, force-retire any document, configure AI check cadence, manage deployment settings |
| **Editor** | Create and edit any document; view version history and diffs; restore prior versions; trigger and accept/reject AI suggestions |
| **Viewer** | Search and view **published, current** documents only |
| **AI agent** (system actor, not a login role) | Read access to all documents; generates summaries and draft/edit suggestions; **cannot publish, delete, or directly modify anything** — all output routes through an Editor or Admin |

## Permission matrix

| Action | Admin | Editor | Viewer |
|---|:---:|:---:|:---:|
| View published documents | ✓ | ✓ | ✓ |
| Create / edit any document | ✓ | ✓ | – |
| View version history & diffs | ✓ | ✓ | – |
| Restore a prior version | ✓ | ✓ | – |
| Delete a document | ✓ | – | – |
| Force-retire a document | ✓ | – | – |
| Create / deactivate users, change roles | ✓ | – | – |
| View/export audit log | ✓ | – | – |
| Trigger AI summary / review / on-demand check | ✓ | ✓ | – |
| Accept or reject an AI suggestion | ✓ | ✓ | – |
| Configure scheduled AI check cadence | ✓ | – | – |
| Manage deployment settings (env, upgrades, backups) | ✓ | – | – |

## Decision log

| Date | Decision | Rationale |
|---|---|---|
| 2026-07-13 | Target SOC 2 / ISO 27001 software companies first; classical QMS later; core standard-agnostic | User decision; niche is vacant, buyer is developer-adjacent |
| 2026-07-13 | Hybrid architecture: DB canonical + one-way git/Markdown export | Pure git fails UX, pure DB fails portability; bidirectional sync is a documented failure mode |
| 2026-07-13 | TypeScript, full stack | Largest contributor pool; editor ecosystem is JS regardless |
| 2026-07-13 | Dogfood solo/internally first | Shapes the v1 scope cut |
| 2026-07-13 | Permissive license: Apache-2.0 code, CC BY 4.0 templates | Differentiates from Comp AI (AGPL); accepted SaaS-resell risk |
| 2026-07-13 | Schema audit-shaped from day one; write-once evidence tables even where UI is deferred | Event-sourced records can't be retrofitted |
| 2026-07-13 | Integer version numbers for published versions | Auditor-friendly; simpler than semver for prose |
| 2026-07-13 | Export runs automatically on publish, with manual rebuild | Evidence should never depend on remembering to export |
| 2026-07-13 | ~~Stack: Node 22 + Hono + Drizzle/SQLite + React/Vite~~ **Superseded same day** → **Next.js single image + PostgreSQL + Prisma + pg-boss + Auth.js (OIDC/SAML + credentials)** | Postgres FTS + pg-boss keep the footprint at exactly two services while enabling v1 search, jobs, and org SSO; one deployable |
| 2026-07-13 | ~~Explicit "Make effective" approval flow in v0.1~~ **Superseded same day** → **v1 lifecycle is Draft → Published → Retired; approval workflows are Phase 2** | Ship the daily-use writing tool first; dormant approval/evidence tables in v1 migrations make Phase 2 UI work, not a migration |
| 2026-07-13 | AI agent assistance (summarize, draft/rewrite, review-as-diff, scheduled + on-demand checks; suggestion-only via suggestions queue) is core v1 scope | "Fun and easy" depends on it; suggestion-only design keeps published docs risk-free and counters AI-policy-mill distrust |
| 2026-07-13 | Import connectors (Confluence/Notion/BookStack/Jira) deferred to v1.x; v1 ships Markdown/zip import | Dogfood the core first; prioritize connectors by where real content lives |
| 2026-07-13 | Multi-tenant groundwork limited to `org_id` on tenant-scoped tables in v1 | Cloud SaaS is Phase 5; avoid painting into a corner without building for a phase that isn't scheduled |
| 2026-07-13 | Name "Bunsho" (Japanese 文書, "document") | Etymology (文 writing + 書 write/document) encodes the thesis — writing and the controlled document are one; connotes formal/official documents, the register Bunsho serves |
| 2026-07-14 | Keep Prisma as the ORM/migration layer (reaffirmed at M0 build) | Matches the stack and the largest TS contributor pool. Trade-off accepted: immutability/write-once invariants aren't expressible in Prisma's schema and ship as raw-SQL trigger migrations alongside the generated init migration |
| 2026-07-14 | Invariants enforced by DB triggers (`*_audit_triggers` migration): published `document_versions` content-immutable (only `retiredAt`/`supersededAt` may transition); `audit_log` append-only; `approvals`/`reviews`/`acknowledgments` write-once. Published versions remain DELETE-able so an Admin document delete cascades; the deletion is recorded in the immutable `audit_log`. Consequence: an org (and any doc carrying evidence rows) cannot be hard-deleted once audited — accepted, matching "history retained indefinitely" | Defend the audit story at the database layer, not just in app code; the delete/cascade seam keeps the single-org v1 clean while making evidence non-erasable |
| 2026-07-14 | Auth.js adapter tables (accounts/sessions/verification) deferred from the M0 first migration to M1 | They are mutable auth plumbing, not write-once evidence, so adding them in M1 is additive — not a retrofit of history. The audit-shaped domain schema ships complete in M0 |
| 2026-07-14 | **M1: Auth.js runs JWT sessions with NO adapter tables** (supersedes the row above). Role + `active` resolved fresh from the DB on every request via `requireUser`; passwords hashed with Node's built-in `scrypt`; `trustHost: true` for self-hosted; OIDC users provisioned as `viewer` on first sign-in | The Credentials provider is incompatible with Auth.js database sessions, so JWT is required and the `Account`/`Session` tables are unnecessary. Reading role/active per request keeps deactivation and role changes effective despite stateless tokens. scrypt avoids a native dependency (smallest-footprint principle) |
| 2026-07-14 | **M1: audit invariants defended in depth** — a DB trigger blocks any UPDATE/DELETE on `audit_log`, and independently the SHA-256 hash chain (`hash = sha256(prevHash ‖ canonical(row))`, per-org advisory-lock-serialized appends, hash computed pre-insert since the row is UPDATE-blocked) makes tampering detectable by `verify-audit` even if a trigger were bypassed | Two independent layers realize tamper-*evidence*: the trigger is the first line, the chain is the proof. Verified live: intact chain passes, a superuser-bypassed edit is caught with a non-zero exit |
| 2026-07-14 | **M2: drafts are unpublished `document_versions`** (`publishedAt` null, mutable — the freeze trigger only locks published rows); `markdown`/`contentSha` nullable (set at publish); **at most one open draft per document** enforced by a trigger, not a partial unique index | A partial unique index would register as Prisma schema drift and fail CI's drift check; a trigger is drift-invisible and consistent with the other invariants. The nullable columns are honest about draft state rather than using sentinel values |
| 2026-07-14 | **M2: TipTap 2.x pinned to exact versions** (StarterKit + table family; no cell-merge extension); editor extensions live behind one `buildEditorExtensions()` wrapper; **autosave writes ProseMirror JSON to the draft and is NOT audit-logged** | Pinning mitigates TipTap license/vendor drift; no cell-merge keeps Markdown faithful. Keeping keystroke-level autosaves out of the audit log keeps the chain meaningful — controlled-document events (publish/retire) are what auditors read |
| 2026-07-14 | **M3: publish freezes a hand-written, deterministic ProseMirror→GFM Markdown serializer output + SHA-256** on the draft version (which then becomes immutable); prior current version is superseded and the document's `currentPublishedVersionId` projection advances, all in one audited transaction | Hand-rolled serializer (unit-tested for byte-identical output) gives full control over GFM table rendering and escaping, which prosemirror-markdown lacks; determinism is a hard requirement. Publishing a draft is allowed by the freeze trigger because the row isn't yet published at update time |
| 2026-07-14 | **M3: editing a published doc forks a new draft**; **restore stages a prior version into a draft** (never auto-publishes); **retire** is Admin-only and sets `retiredAt` on both document and current version; diff is jsdiff word-level on frozen Markdown | Fork/restore-to-draft keep a human gate before any new controlled version exists; document-level `retiredAt` makes lifecycle state and Viewer search a simple query. Verified live end-to-end: publish→supersede→fork→republish→diff→restore→retire with the audit chain intact |
| 2026-07-14 | **M4: full-text search via inline `to_tsvector`/`websearch_to_tsquery`** over current published Markdown + title + doc code (no stored tsvector column / GIN index yet); role-aware — Viewers only ever match current published, non-retired docs | At v1 volumes (dozens–hundreds of docs) inline FTS is fast and avoids a generated column that would either fight Prisma's drift check or need `Unsupported` typing; a stored GIN-indexed column is a later optimization. Draft *bodies* aren't indexed (no frozen Markdown) — drafts match by title/code |
| 2026-07-14 | **M5: AI is suggestion-only and off unless `ANTHROPIC_API_KEY` is set** (`AI_ENABLED=false` force-disables); model `claude-opus-4-8`. Staleness + broken-reference checks are **deterministic** (no LLM); summary/review call the LLM. Everything lands in a per-document suggestions queue; **accepting a content suggestion forks/updates the draft → a new version**, audit-logged `suggestion_accepted` as AI-originated with the approving human recorded. Dedup against *pending* only. `maxPerRun` caps each run. Scheduled checks run on **pg-boss** (own `pgboss` schema — no Prisma drift), started via `instrumentation.ts`, gated by `AI_SCHEDULED_CHECKS`; on-demand checks run inline | Deterministic checks realize the audit-critical accept→version→attribution loop and are fully live-verified without an API key. **Deferred: LLM-generated *content* rewrites** — the serializer is one-way, so applying LLM Markdown would need a Markdown→ProseMirror parser; until then the accept-content path is exercised via suggestions whose payload already carries ProseMirror JSON, and LLM review returns advisory notes. pg-boss/schedule opt-in keeps CI/dev clean |
| 2026-07-14 | **M6: one-way git/Markdown export is a deterministic projection of the DB** — each doc exported as `<folder>/<code>.md` (YAML front matter + frozen body + change-log table); the git rebuild replays every published version as one commit (author timestamp = publish time, fixed identity) so re-running yields identical commit SHAs; zip download uses fixed entry dates. Import parses Markdown→ProseMirror (markdown-it) and lands content in **Draft** (never auto-published). Starter pack = 8 original SOC 2 templates (CC BY 4.0), seeded idempotently as Drafts | Determinism satisfies the byte-identical-rebuild requirement. **Building the Markdown→ProseMirror parser here also closes the M5 gap** — LLM content rewrites can now be applied to drafts (wiring only). Verified live: templates idempotent, two git rebuilds → same HEAD SHA, zip round-trips, import → Draft; audit chain intact |
| 2026-07-14 | **M7: v1 release packaging** — one-command Docker Compose quickstart (env-wired `AUTH_SECRET`/AI/OIDC passthrough; `migrate` one-shot applies schema upgrades before the app starts); ghcr.io image published on version tags; `openssl` added to the image for Prisma engine detection; Apache-2.0 `LICENSE` (+ CC BY 4.0 for templates); `docs/INSTALL.md`. Phase 1 (M0–M7) feature-complete; dogfooding is next | Verified live: `docker compose up --build` brings up db → migrate → app; in-container health, first-run `/setup`, and auth CSRF all work; `verify-audit` runs clean. Later phases (1.x connectors, 2 approvals, 3 notifications, 4 audit/PDF, 5 SaaS) remain future work |
| 2026-07-14 | **UI redesign as a pure presentation layer: "carbon & gold" design system** — Tailwind CSS v4 + `motion` + `lucide-react` + self-hosted `next/font` (Archivo display / Inter body / IBM Plex Mono for doc codes); dark carbon theme with champagne-gold accents; hanko motif: statuses as seal chips, **publish plays a one-time gold seal-stamp celebration** (driven by a `?published=` redirect param, stripped on play); persistent role-aware sidebar shell via `(app)`/`(auth)` route groups (URLs unchanged); gamification capped at progress-and-delight — no points/XP/badges. All animation respects `prefers-reduced-motion` | The old inline-style UI read as unusable; the product promises "fun and easy." Build-time deps keep the two-service footprint; TipTap extensions/serializer untouched so frozen Markdown determinism holds; dark-only commits to one polished look while CSS tokens leave a light theme open. Incidental fix: pg-boss stubbed out of the edge instrumentation compile (`next.config.mjs`) so `next dev` works outside Docker |
| 2026-07-15 | **Cream "paper" document surface retracted — editor and published view render on a dark surface** (`--paper: #211d18`); the `paper` token and Card variant names stay | User feedback: the light editor canvas was too bright against the dark shell. Token-level change only, so both surfaces stay consistent and a light theme remains possible later |
| 2026-07-15 | **Profile page (`/profile`) with per-user activity read straight from the audit log** (no separate feed table). **Document detail gets a "Last change" right rail** linking to a new **`/documents/[id]/history` page**, where the change-log and diff/restore tables moved from the detail page | Audit log as the single activity source keeps the feed tamper-evident for free; splitting history out declutters the document view while keeping diffs one click away |
| 2026-07-15 | **Document attachments (files + inline editor images)** — `attachments` table stores bytes in Postgres (`bytea`, 20 MB/file cap) with sha256 fingerprint; write-once audit actions `attachment_added`/`attachment_deleted`; served only via `/api/attachments/[id]` (auth + org-scoped on every read; viewers reach only attachments of current published docs; safe raster images render inline, everything else — incl. SVG — forces download with `nosniff` + `default-src 'none'` CSP). Server-action body cap raised to 25 MB | Attachments in Postgres keep the deployment at exactly two services and inside `pg_dump` backups; inline-as-GFM-image keeps frozen Markdown faithful. Trade-off accepted: exported Markdown references app-relative attachment URLs, so the git export is not self-contained for images — revisit if export portability becomes a requirement |
| 2026-07-15 | **Document types removed entirely** — `DocumentType` enum and `documents.type` dropped (migration `drop_document_type`), along with the Type field, search facet, table column, and front-matter line. Auto-assigned doc codes now use a single `DOC-` prefix instead of per-type (`POL-`/`SOP-`/`WI-`/`STD-`); existing codes keep historical prefixes and the manual field still accepts any 2–6 letter prefix. Starter templates seed with allocated `DOC-` codes, idempotency keyed on **title** rather than doc code. **Sidebar gains a primary "New document" button** (editors/admins only) | User decision: classification added friction at creation without earning its keep — the folder already carries the grouping a single-org library needs. Sharing one code sequence with user documents means a hardcoded template code would collide with whatever the org already created and silently skip that template. Accepted trade-off: existing type values are unrecoverable, and doc codes no longer encode the kind of document at a glance |
| 2026-07-15 | **draw.io diagrams as first-class editor blocks, air-gapped by default** — TipTap `drawio` atom node whose single attribute is the *editable SVG* data URI (`xmlsvg`: the rendered SVG carries its source mxfile XML), so one string is both picture and re-editable source. Full-screen embedded draw.io (JSON postMessage: `init`→load, `save`→export `xmlsvg`→apply). The webapp is **vendored into the image** at build time (`scripts/fetch-drawio.ts` via `prebuild`/`predev`: pinned v30.3.11 `draw.war`, sha256-verified, extracted to gitignored `public/drawio/`) and served same-origin with `stealth=1`, so drawing needs **zero external network**; `NEXT_PUBLIC_DRAWIO_URL` (build-time) can point elsewhere, `DRAWIO_SKIP_FETCH=1` skips vendoring. Serializes as a plain GFM image; the importer routes SVG data-URI images back to `drawio` blocks (markdown-it `validateLink` extended to allow `data:image/svg+xml`, safe because they render via `<img>` where SVG scripts never execute) | Diagrams-as-data keeps versions self-contained: the diagram lives inside the frozen Markdown bytes, so diffs and the git export need no side files. Bundling the editor keeps the two-service deployment honest for offline/compliance environments — the alternative (public embed.diagrams.net) leaks availability and metadata to a third party. Trade-offs accepted: app image grows ~150 MB, and a changed diagram shows as one long data-URI line in diffs |
| 2026-07-16 | **Prisma 7 migration, driven by a broken `main`** — dependabot's Prisma 6→7, Next 15→16, and a lone `@tiptap/starter-kit` 2→3 bump each landed a breaking change without its migration, taking all three CI jobs red. Fixes: `prisma.config.ts` now carries the datasource URL (v7 rejects `url` in `schema.prisma`), reading `process.env` directly rather than Prisma's `env()` helper, which throws eagerly at module load and would break the two places that generate with no database in scope (Docker builder, CI `postinstall`); `@prisma/adapter-pg` wired into `db.ts` and `verify-audit.ts` (v7 dropped the Rust engine — a driver adapter is mandatory); `dotenv` added (v7 stopped auto-loading `.env`); `@prisma/client-runtime-utils` declared explicitly, since pnpm nests transitive deps where the generated client at `src/generated/prisma` — outside `node_modules` — can't resolve them; `dev`/`build` pass `--webpack` because Next 16 defaults to Turbopack and errors on a webpack config; `@tiptap/starter-kit` reverted to 2.27.2 and dependabot now ignores `@tiptap/*` majors | The TipTap family is pinned as a set on purpose (2026-07-14): bumping starter-kit alone put two copies of TipTap in the tree and v3's `ChainedCommands` lost `toggleBold`/`toggleItalic`/`toggleHeading`, so the toolbar stopped typechecking — majors arrive ungrouped, which is how one slipped through. The `--webpack` pin matters: the alias it preserves is conditional on `nextRuntime === "edge"`, which Turbopack's static `resolveAlias` can't express, and the tempting empty `turbopack: {}` would silence the error by *dropping* the alias and reintroducing the edge-compile bug it exists to prevent. Verified against real infrastructure: 88 unit + 294 integration tests on real Postgres, production build, and `verify-audit` on a scratch DB. Deferred: a genuine TipTap v3 upgrade, porting the edge alias to Turbopack, and removing the now-vestigial `openssl` from the Dockerfile |
| 2026-07-15 | **Test strategy: the invariants are tested against a real Postgres, not a mocked Prisma** — the suite splits into two Vitest projects: `unit` (pure logic, no infrastructure, `pnpm test`) and `integration` (`*.db.test.ts`, `pnpm test:db`), with `pnpm test:all` / `pnpm coverage` over both. Integration tests run against a **scratch database** (`TEST_DATABASE_URL`, default `bunsho_test`), auto-created and migrated by a global setup, `TRUNCATE`d between tests, single-fork so files never race. A guard refuses any database whose name doesn't mark it as scratch. CI gains an `integration` job with a `postgres:17` service. Coverage of `src/lib` went 25% → 92% (the `lib` core itself 99.7%); the remaining gap is `editor/drawio.tsx`, a React/TipTap view needing a DOM | The guarantees that make Bunsho a *control* system live in the database, not the application: the append-only `audit_log` trigger, the frozen-published-version trigger, one-draft-per-document, transaction rollback, and org scoping. A mocked client asserts call shape and would have proved none of them — so the tests that matter most are exactly the ones a mock cannot write. TRUNCATE (not DELETE) is required: the write-once triggers reject row deletion, and TRUNCATE doesn't fire row-level triggers, so the guarantee stays armed for the tests that assert it. Verified: publish/retire/restore, chain tamper-evidence, and Viewer search scoping all hold; dropping the scratch DB and re-running rebuilds it from nothing. Two Prisma translation quirks recorded for future test authors: a trigger raising `unique_violation` surfaces as P2002 with the trigger's message **discarded**, while `restrict_violation` is unrecognized so its message survives |
| 2026-07-15 | **Planning docs retired; `DECISIONS.md` + `CLAUDE.md` are the documentation set** — this file carries the decision log, roles, and permission matrix; `CLAUDE.md` carries architecture and invariants for contributors and agents | Phase 1 is built, and a large spec describing shipped code drifts from it. Keep the durable *why* (this log) and drop the vision/market/persona prose |
| 2026-07-18 | **Re-file a document into a folder from two places — the edit page and Explorer drag-and-drop** — one lib function `moveDocument(orgId, documentId, folderId)` (org-scopes both the document and the target folder, sets `folderId`, and clears `parentId`) behind one bound server action `moveDocumentAction(documentId, folderId)`. **Editing:** the edit-page header gains a `FolderPicker` (client) that saves the instant a folder is chosen — no button, matching the editor's own save-on-idle register. It shows only for top-level documents; a nested page shows "Nested page — filed with its parent" instead, because `documents_child_has_no_folder` forbids a child carrying a folder. **Explorer:** `ExplorerTree` becomes a client component with native HTML5 drag-and-drop — page rows are drag sources, folder and "Unfiled" summaries are drop targets, a custom `application/x-bunsho-doc-id` MIME so stray link/file drags are ignored, and per-target hover highlight; the deepest drop target wins (`stopPropagation`). Dropping a page on a folder files it there; on "Unfiled" moves it out to top level; "Unfiled" is always shown to managers so there is a target to move a page out even when nothing is currently unfiled. Dragging a nested page onto a folder detaches it from its parent (the two are mutually exclusive) and files it — a file-manager gesture; the moved page's own children follow it. Like folder create/rename, a move is **filing, not controlled content, so it is not audit-logged**. The pure Explorer view-model (types + `countPages`/`assembleExplorerTree`) split out to `src/lib/explorer-tree.ts` so the client tree can import them without dragging Prisma (and `pg`, which has no browser build) into the client bundle; `explorer.ts` re-exports it, so existing importers are unaffected | Filing lived only at creation before; a document could never be re-filed without recreating it. Two entry points because they serve different moments — the picker for when you are already in a document, drag-and-drop for when you are surveying the library. `moveDocument` clears `parentId` on every move so the one code path satisfies the CHECK whether the source was a root page or a nested one, and so the gesture reads the way it does in a file manager. Native DnD over a library: the smallest-footprint rule (app + Postgres, no extra services or deps) rules out a drag-and-drop package, and the tree is not large enough to need virtualization. Verified: 88 unit + 310 integration tests on real Postgres (8 new `moveDocument` cases — file, unfile, detach-on-file, subtree-follows, updatedAt bump, and both cross-tenant guards), lint, typecheck, and a production build all green. Boundary: the live browser drag gesture itself was not exercised end-to-end (this project does not automate React-view testing — the same acknowledged gap as `drawio.tsx`) |
| 2026-07-17 | **Save-only pages — the Draft/Published distinction leaves the UI** — a page is simply *saved*, with no explicit publish step. The editor now runs two timers: the 800 ms autosave writes the draft as before, and a longer idle timer (`COMMIT_MS`, 5 s of settled typing) freezes it as a version via the new `commitAction`, which calls `publishDocument(..., { ifDraftOpen: true })` — a no-op returning `null` when a prior commit or another tab already closed the draft, so a background timer never throws. `saveDraftAction` re-forks a draft first (`getOrCreateDraft`) because each commit closes the one it froze. The editor unmount flush chains save-then-commit so navigating away inside the idle window still lands a version. UI removed: the Publish card and change-note field, `LifecycleStepper`, the publish celebration `SealStamp`, and every Draft/Published status badge on the dashboard, library, explorer tree, search results, and detail/history headers. `StatusSeal` keeps only `retired` (an Admin act) and `current`/`superseded` (a version's standing in history). **The data model is unchanged** — versions, the frozen-published trigger, `contentSha`, the audit spine, and both export paths still work exactly as before; publish is still the audit-logged event, now fired on idle instead of a button. Viewer visibility is unchanged: a page reaches Viewers once its first idle commit lands (`currentPublishedVersionId` set) | The publish gesture earned nothing for a single-org authoring tool — an editor thinks in "save," not "cut a release," and every save already froze an immutable, diffable, exportable version. Keeping the lifecycle *machinery* while dropping its *vocabulary* preserves the audit and export guarantees at zero cost. `COMMIT_MS` ≫ `AUTOSAVE_MS` on purpose: each commit costs a version row, an audit entry, and a git-export commit, so a session must coalesce into a handful rather than one per keystroke-debounce. `publishDocument` gained function overloads so the ~25 existing callers that never pass `ifDraftOpen` keep their non-null return type; only the idle-commit path sees `null`. Verified: 89 unit + 303 integration tests on real Postgres, lint, and typecheck all green |
